# OpenCode Global Config

This directory is intended to be symlinked to the OpenCode global config location.

Project-level `.opencode/` directories remain project-owned and should not be overwritten by this global adapter.
