# OpenCode Adapter Entry

Read and follow the canonical AI Harness entry point:

`~/.ai-harness/AGENTS.md`

OpenCode-specific notes:

- This file exists so OpenCode can discover the global harness entry point through `~/.config/opencode/AGENTS.md`.
- Project-level `AGENTS.md` files remain project-owned and should be followed first when present.
- OpenCode-specific tools, plugins, commands, and agent profiles live under `~/.ai-harness/adapters/opencode/`.
- Do not treat this adapter file as project truth.
