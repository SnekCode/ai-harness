// Placeholder OpenCode custom tool: read/update project goal state.
// Implementation target: operate on project .harness/goal.yaml and .harness/state.json.

export async function goalState() {
  return {
    status: "not_implemented",
    next: "Implement goal.yaml/state.json reader and verifier"
  };
}
